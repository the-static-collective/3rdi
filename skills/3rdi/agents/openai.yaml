interface:
  display_name: "3rdi"
  short_description: "See from one observer-local cut without hindsight"
  default_prompt: "Use $3rdi to show what this observer could lawfully know at the selected cut without importing hindsight."

policy:
  allow_implicit_invocation: true
