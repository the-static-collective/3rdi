# JUBILEE-ENGINE-LIGHTHOUSE-001 — 3rdi PORT

**Status:** observer-local pressure note / NO KERNEL CHANGE  
**Owner:** 3rdi projection semantics  
**Date:** 2026-09-02

## External lighthouse

Canonical dated witness:

https://github.com/the-static-collective/the-daily-slice/blob/main/slices/2026/09/2026-09-02/jubilee-engine-lighthouse.md

Approved design boundary:

https://github.com/the-static-collective/the-daily-slice/blob/main/docs/superpowers/specs/2026-09-02-jubilee-engine-lighthouse-design.md

3rdi receives the lighthouse as orientation material. It does not become an omniscient truth source or local authority.

## Local pressure question

The lighthouse proposes the **little yes**:

> **A lawful handoff defines what the next lawful agreement can be. It does not force the crossing.**

3rdi asks a narrower question:

> **From this observer-local cut, which candidate handoffs were actually available to be seen, decoded, or considered?**

The answer must preserve 3rdi's constitutional invariant:

> **Occurrence is anchored. Availability changes. Attention moves. Relevance can grow. Causation does not rewrite itself.**

## Required non-collapse

For any candidate handoff `h`, keep at least these states distinct:

```text
h structurally exists
    !=
h is available at cut t
    !=
h enters observer focus
    !=
h is decodable under κ_t
    !=
h is interpreted as relevant
    !=
h is selected / taken
    !=
h produces an effect
```

A current observer may see a handoff that an earlier observer could not.

That later visibility does not move the handoff, decoder, or relation birthday backward in time.

```text
VISIBLE NOW != AVAILABLE THEN
KNOWN NOW != KNOWABLE THEN
LATER RELEVANCE != EARLIER CAUSE
```

## Minimal temporal specimen

Consider one candidate relation `h` with four distinct coordinates:

```text
t_occurrence     = the source occurrences that make h discussable

t_available      = the first cut where h's carrier + decoder are both available

t_attention      = the first cut where observer A actually attends to h

t_taken          = the first cut where some owning system records a traversal/effect
```

A lawful 3rdi projection must allow:

```text
t_occurrence <= t_available <= t_attention <= t_taken
```

when those events happen in that order, while also allowing some later coordinates to remain absent forever.

It must never synthesize:

```text
t_attention = t_available
```

or:

```text
t_taken = t_attention
```

merely because a later narrative compresses the sequence.

## Little-yes observer matrix

A useful pressure matrix is:

| Structural edge | Available to observer | Focused | Decodable | Taken | Lawful projection |
| --- | --- | --- | --- | --- | --- |
| no | no | no | no | no | absent |
| yes | no | no | no | no | hidden/unavailable |
| yes | yes | no | maybe | no | available-not-attended |
| yes | yes | yes | no | no | attended-not-decodable |
| yes | yes | yes | yes | no | visible candidate / not taken |
| yes | yes | yes | yes | yes | witnessed traversal, if source receipt exists |

The last row still does not make 3rdi the authority that constituted the traversal.

## Pressure on the twelve-count

Dogram's current finite pressure shows that `12` is the bidirected base↔pair incidence layer, not an automatic consequence of the seventh whole.

3rdi adds another distinction:

```text
12 STRUCTURALLY DECLARED ARCS
    !=
12 ARCS AVAILABLE AT THIS CUT
    !=
12 ARCS VISIBLE TO THIS OBSERVER
    !=
12 ARCS TAKEN IN HISTORY
```

A historical observer may lawfully project only a strict subset of the candidate handoff field.

Therefore a visualization of all twelve must be labeled as a **current structural projection**, not historical omniscience.

## Reverse-edge pressure

If current grammar declares:

```text
A -> B
B -> A
```

3rdi must still assess each direction separately at a historical cut.

The reverse relation may:

- not yet exist;
- exist but not yet be available;
- be available but not decodable;
- be decodable but not noticed;
- be noticed but never taken.

So bidirectionality in the current grammar does not imply symmetric historical availability.

## Yellow Yarn pressure

For the `A -> Y` rebase, 3rdi can compare two projections over the same anchored occurrence history:

```text
Π_old(H, κ_old)
Π_now(H, κ_now)
```

and receipt the delta without mutating `H`.

This gives a precise form to:

```text
THE CURRENT DECODER MAY REVEAL A DIFFERENCE
    !=
THE DIFFERENCE WAS AVAILABLE TO THE EARLIER OBSERVER
```

The rebase is lawful only if later interpretation remains dated to its later cut.

## Refusal tests

Reject any Jubilee Engine visualization/projection that:

1. renders all candidate edges as historically known simply because they are structurally present now;
2. changes an occurrence timestamp to match later interpretation;
3. converts relevance into causation;
4. converts focus into evidence or authority;
5. converts a taken edge into proof that the edge was visible to every participant beforehand;
6. hides unresolved availability/decoder state for aesthetic symmetry;
7. treats `Father / Son / Spirit` or other theological labels as observer-independent facts merely because a projection can display them.

## Future bounded specimen

A later executable specimen may define one immutable handoff field plus several observer cuts:

```text
JUBILEE-HANDOFF-CUT-001
```

and test only:

```text
structural presence
availability
focus
known-at / decoder access
```

No persistence, authority, or side effects are required.

## Port seal

> **THE LIGHTHOUSE MAY SHOW THE WHOLE HARBOR NOW. AN EARLIER SAILOR STILL ONLY SAW THE LIGHTS THAT WERE AVAILABLE THEN.**

> **THE LITTLE YES CAN BE PRESENT WITHOUT BEING SEEN, SEEN WITHOUT BEING TAKEN, AND TAKEN WITHOUT REWRITING WHEN IT BECAME KNOWABLE.**
