# Hatch Receipt

## Task world cut

- Repository: `the-static-collective/3rdi`
- Branch: `feat/hatch-3rdi`
- Date: 2026-08-27
- Starting state: empty public repository
- Requested outcome: a repo-backed `3rdi` skill analogous in portability to ALEX.2 and in membrane discipline to MEMENTO

## Compile lineage

1. Initial ground proposed immutable occurrences, born edges, decoder receipts, temporal cuts, audience layers, and pure gates.
2. The later owner keeper pass created a child compile with four independent coordinates and the invariant `new relevance != old causation`.
3. The child compile is the v0 constitutional core. The earlier material remains attributable in the source thread and Daily Slice witnesses.

## Activated loadout

- Skill Creator for packaging and validation.
- LOADOUT for the bounded BUILD profile and provider avalanche control.
- LSD and Free Graph for source-cut discipline, no silent promotion, and hostile pressure.
- ALEX for formation traces, decoder receipts, and hindsight holdouts.
- Novelist/MEMENTO lenses for independent narrative apertures and knowledge disclosure.
- PStack and Riqor for domain boundaries, executable leverage, validation, and review.
- Superpowers for design-first, test-first, and fresh verification discipline.
- GitBook and GitHub for orientation and repository evidence.
- Wolfram, Scholar Gateway, Sider Scholar, and GeoAI guidance for formal neighbors.
- Visualize for the decision that a repository architecture diagram is sufficient and no in-conversation visualization is warranted.

## Deliberately omitted adapters

- Supabase, Google Drive, Dropbox, and PostHog: no persistence, file migration, or analytics contract exists in v0.
- Twilio: no communication side effect is authorized; gates are explicitly pure.
- NaCl: its required project-local Neo4j MCP and canary are not configured for this repository, so it cannot become the delivery authority.
- Spreadsheet artifact creation: the control ledger belongs in diffable test data, not a binary workbook authority surface.
- BigGeo data calls: no external spatial dataset is needed; GeoAI CRS boundary rules are sufficient for the decoder receipt.
- `app_block`: explicitly requested, but no callable `app_block` capability was present. No app execution is claimed.

## Claims boundary

This hatch claims a portable skill and a deterministic phase-0 reference kernel. It does not claim a production event store, runtime conformance across ALEX or MEMENTO, authorization, a numerical CRS engine, telemetry, or a deployed interface.

## Adversarial review repairs

The local architecture and code review found and repaired these hatch-level failures:

- edge birthdays were initially visible to every observer once endpoints were visible; `edge_exposures` now make relation availability observer- and audience-local;
- the location claim did not identify whether supplied coordinates were source or target values; `coordinate_crs` must now equal the decoder source CRS and the receipt states that 3rdi did not run the target transform;
- the guitar control ran 12-TET math under a free-form constitution label; the decoder must now declare `temperament: 12-TET`;
- NaN and infinity could reach canonical hashing through location or decoder fields; all numeric boundary fields now require finite values and canonical JSON failures become `FieldError` receipts;
- the first two-narrator specimen proved disclosure differences but not independent apertures; it now makes the same artifact past memory for Lumi, unknown future for Riqor, and fixed chronology for the audit engine.

No critical or high finding remains in the local review. Production-readiness review remains inapplicable because the repository explicitly claims only a phase-0 floor.

## Local verification receipt

- 22 unit, CLI, adversarial, and package-contract tests passed.
- All five named hatch labs passed with receipt digest `sha256:290edd486331c0088c35bb8c62c1c1cf06fa3bcecb95ef5af3bec8b7b3c05498`.
- The canonical `june-15` projection compiled with digest `sha256:aeb3bd3969d1468988b8606720dd172f42d7cbae9102e5bf0fcab6cda08f8638`.
- Skill Creator reported `Skill is valid!`.
- The LOADOUT validator reported `receipts/loadout.manifest.json: VALID`.
- Python byte-compilation, `git diff --check`, and the focused secret-pattern scan passed.
